openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
  - name: staff
  - name: entities
  - name: notifications
  - name: sync
  - name: system
  - name: files
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /v1/auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Staff login with issued code
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Authenticated session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /v1/auth/bootstrap:
    post:
      operationId: bootstrapAdministrator
      tags: [auth]
      summary: Create the first administrator for an empty tenant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, code]
              properties:
                name: { type: string }
                code: { type: string, minLength: 4, maxLength: 4 }
                tenantId: { type: string }
                firstName: { type: string }
                lastName: { type: string }
      responses:
        "201":
          description: Bootstrap session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "409":
          description: Administrator already exists
  /v1/auth/bootstrap-status:
    get:
      operationId: getBootstrapStatus
      tags: [auth]
      summary: Check whether the default tenant already has an administrator
      responses:
        "200":
          description: Bootstrap availability
          content:
            application/json:
              schema:
                type: object
                required: [hasAdministrator]
                properties:
                  hasAdministrator: { type: boolean }
  /v1/auth/refresh:
    post:
      operationId: refreshSession
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refreshToken]
              properties:
                refreshToken: { type: string }
      responses:
        "200":
          description: Refreshed session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /v1/auth/me:
    get:
      operationId: getCurrentStaff
      tags: [auth]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Current staff account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Staff"
  /v1/staff:
    get:
      operationId: listStaff
      tags: [staff]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: status, in: query, schema: { type: string } }
      responses:
        "200":
          description: Staff accounts
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Staff" }
    post:
      operationId: createStaff
      tags: [staff]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffInput"
      responses:
        "201":
          description: Created staff account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Staff"
  /v1/{entity}:
    parameters:
      - { name: entity, in: path, required: true, schema: { type: string } }
    get:
      operationId: listEntityRecords
      tags: [entities]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: entity, in: path, required: true, schema: { type: string } }
        - { name: projectId, in: query, schema: { type: string } }
        - { name: development, in: query, schema: { type: string } }
        - { name: status, in: query, schema: { type: string } }
      responses:
        "200":
          description: Entity records
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EntityRecord" }
    post:
      operationId: createEntityRecord
      tags: [entities]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EntityInput"
      responses:
        "201":
          description: Created entity record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntityRecord"
  /v1/{entity}/{id}:
    parameters:
      - { name: entity, in: path, required: true, schema: { type: string } }
      - { name: id, in: path, required: true, schema: { type: string } }
    get:
      operationId: getEntityRecord
      tags: [entities]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: entity, in: path, required: true, schema: { type: string } }
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Entity record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EntityRecord" }
    patch:
      operationId: updateEntityRecord
      tags: [entities]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EntityInput"
      responses:
        "200":
          description: Updated entity record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EntityRecord" }
    delete:
      operationId: deleteEntityRecord
      tags: [entities]
      security: [{ bearerAuth: [] }]
      responses:
        "204": { description: Record deleted }
  /v1/notifications:
    get:
      operationId: listNotifications
      tags: [notifications]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Notifications for the current actor
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Notification" }
  /v1/notifications/{id}/read:
    post:
      operationId: markNotificationRead
      tags: [notifications]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Updated notification
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Notification" }
  /v1/devices/token:
    post:
      operationId: registerDeviceToken
      tags: [system]
      security: [{ bearerAuth: [] }]
      summary: Register or refresh an Expo push token for the current staff member
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token]
              properties:
                token: { type: string }
                platform: { type: string }
      responses:
        "201":
          description: Registered device token
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DeviceToken" }
        "400":
          description: Invalid Expo push token
  /v1/push-deliveries:
    get:
      operationId: listPushDeliveries
      tags: [system]
      security: [{ bearerAuth: [] }]
      summary: List recent push delivery attempts for management
      parameters:
        - { name: notificationId, in: query, schema: { type: string } }
      responses:
        "200":
          description: Recent delivery attempts
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PushDelivery" }
        "403":
          description: Elevated management access required
  /v1/push-smoke-test:
    post:
      operationId: createPushSmokeTest
      tags: [system]
      security: [{ bearerAuth: [] }]
      summary: Send an alert to the current staging staff member
      description: Available only for the configured staging tenant when staging push smoke tests are explicitly enabled.
      responses:
        "202":
          description: Smoke-test notification queued
          content:
            application/json:
              schema:
                type: object
                required: [notificationId, message]
                properties:
                  notificationId: { type: string }
                  message: { type: string }
        "403":
          description: Elevated management access required
        "404":
          description: Staging smoke tests are disabled for this tenant
  /v1/sync:
    get:
      operationId: pullSync
      tags: [sync]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: since, in: query, schema: { type: string, format: date-time } }
        - { name: entities, in: query, schema: { type: string } }
      responses:
        "200":
          description: Cursor-based changes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncResponse"
  /v1/files/upload-url:
    post:
      operationId: requestFileUploadUrl
      tags: [files]
      summary: Request a direct upload URL for a project file
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FileUploadUrlRequest"
      responses:
        "200":
          description: Presigned upload URL and persistent file envelope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileUploadUrlResponse"
        "400":
          description: Invalid file metadata
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "413":
          description: File exceeds the 50 MB limit
        "415":
          description: Unsupported file type
        "503":
          description: File storage unavailable
  /v1/files/download-url:
    post:
      operationId: requestFileDownloadUrl
      tags: [files]
      summary: Request a temporary download URL for a stored project file
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FileDownloadUrlRequest"
      responses:
        "200":
          description: Temporary presigned download URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileDownloadUrlResponse"
        "404":
          description: File is outside the current tenant
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    Unauthorized:
      description: Authentication required
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    LoginInput:
      type: object
      required: [name, code]
      properties:
        name: { type: string }
        code: { type: string, minLength: 4, maxLength: 4 }
        role: { type: string }
    AuthResponse:
      type: object
      required: [accessToken, refreshToken, staff]
      properties:
        accessToken: { type: string }
        refreshToken: { type: string }
        expiresIn: { type: integer }
        staff: { $ref: "#/components/schemas/Staff" }
    Staff:
      type: object
      required: [id, name, role, position, status, developments]
      properties:
        id: { type: string }
        name: { type: string }
        firstName: { type: string, nullable: true }
        lastName: { type: string, nullable: true }
        role: { type: string }
        position: { type: string }
        status: { type: string }
        developments:
          type: array
          items: { type: string }
    StaffInput:
      type: object
      required: [name, role, position]
      properties:
        name: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        role: { type: string }
        position: { type: string }
        developments:
          type: array
          items: { type: string }
        code: { type: string, minLength: 4, maxLength: 4 }
    EntityInput:
      type: object
      required: [id, state]
      properties:
        id: { type: string }
        projectId: { type: string }
        development: { type: string }
        state: { type: object, additionalProperties: true }
    EntityRecord:
      allOf:
        - $ref: "#/components/schemas/EntityInput"
        - type: object
          required: [entity, createdAt, updatedAt, version]
          properties:
            entity: { type: string }
            createdAt: { type: string, format: date-time }
            updatedAt: { type: string, format: date-time }
            version: { type: integer }
    Notification:
      type: object
      required: [id, target, message, read, at]
      properties:
        id: { type: string }
        target: { type: string }
        message: { type: string }
        detail: { type: string, nullable: true }
        reportId: { type: string, nullable: true }
        read: { type: boolean }
        at: { type: string, format: date-time }
    DeviceToken:
      type: object
      required: [id, tenantId, staffId, token, lastSeenAt, createdAt, updatedAt]
      properties:
        id: { type: string }
        tenantId: { type: string }
        staffId: { type: string }
        token: { type: string }
        platform: { type: string, nullable: true }
        lastSeenAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PushDelivery:
      type: object
      required:
        [id, tenantId, notificationId, status, attemptedAt, createdAt, updatedAt]
      properties:
        id: { type: string }
        tenantId: { type: string }
        notificationId: { type: string }
        tokenId: { type: string, nullable: true }
        staffId: { type: string, nullable: true }
        status: { type: string }
        ticketId: { type: string, nullable: true }
        errorCode: { type: string, nullable: true }
        detail: { type: string, nullable: true }
        receiptAttempts: { type: integer }
        nextReceiptCheckAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        attemptedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SyncResponse:
      type: object
      required: [cursor, records, notifications]
      properties:
        cursor: { type: string, format: date-time }
        records:
          type: array
          items: { $ref: "#/components/schemas/EntityRecord" }
        notifications:
          type: array
          items: { $ref: "#/components/schemas/Notification" }
    FileKind:
      type: string
      enum:
        - room-photo
        - inspection-evidence
        - completion-photo
        - scan
        - procurement-scope
    StoredFile:
      type: object
      required: [id, kind, name, size, contentType, objectPath, createdAt, createdBy]
      properties:
        id: { type: string, format: uuid }
        kind: { $ref: "#/components/schemas/FileKind" }
        name: { type: string }
        size: { type: integer, minimum: 1, maximum: 52428800 }
        contentType: { type: string }
        objectPath: { type: string }
        createdAt: { type: string, format: date-time }
        createdBy: { type: string }
    FileUploadUrlRequest:
      type: object
      required: [kind, name, size, contentType]
      additionalProperties: false
      properties:
        kind: { $ref: "#/components/schemas/FileKind" }
        name: { type: string, minLength: 1, maxLength: 255 }
        size: { type: integer, minimum: 1, maximum: 52428800 }
        contentType: { type: string, minLength: 1, maxLength: 100 }
    FileUploadUrlResponse:
      type: object
      required: [uploadUrl, file]
      properties:
        uploadUrl: { type: string, format: uri }
        file: { $ref: "#/components/schemas/StoredFile" }
    FileDownloadUrlRequest:
      type: object
      required: [objectPath]
      additionalProperties: false
      properties:
        objectPath:
          type: string
          pattern: "^/objects/tenants/"
    FileDownloadUrlResponse:
      type: object
      required: [downloadUrl, expiresIn]
      properties:
        downloadUrl: { type: string, format: uri }
        expiresIn: { type: integer }
    Error:
      type: object
      required: [error]
      properties:
        error: { type: string }
